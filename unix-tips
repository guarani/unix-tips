# Find files recursively
find . -name "*.png"

# Find files recursively (ignore a particular directory)
find . -path ./hello/www -prune -o -name "*.png"

# Grep for a string in all files recursively
grep -ri "*hello*" .
